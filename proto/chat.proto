syntax = "proto3";

package proto;

import "google/api/annotations.proto";

// Types
message Message{
    string sender = 1;
    string text = 2;
    bool identification = 3;
}

// Services

service Chat{
    
    rpc TransferMessage(stream Message) returns (stream Message) {}

}
